[phases.setup]
nixPkgs = ['nodejs_18', 'python39', 'ffmpeg', 'curl']

[phases.install]
cmds = [
    'npm install --omit=dev',
    'pip install -r ai_engine/requirements.txt'
]

[phases.build]
cmds = [
    'mkdir -p uploads outputs logs'
]

[start]
cmd = 'npm start'